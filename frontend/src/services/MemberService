import axios from "axios";

class MemberService {
  async findMember(): Promise<string> {
    let book: string = "";

    try {
      const response = await axios.get<string>("http://localhost:8080/mem/1");

      book = response.data;
    } catch (err) {
      console.error(err);
    }

    return book;
  }
}

export default new MemberService();
